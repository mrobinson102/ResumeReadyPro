
# streamlit_app.py (truncated preview - full code will be in final upload)
# Includes: auth, summary generation, upload resume, prompt lab, job fit analysis,
# admin dashboard, registration, password change, about page.
# PDF export is unicode-safe. CEO image is in /assets.
